Random Sentence Generator

The program takes a grammar file (*.g) and parses it for Grammatical structure of the sentence to be created, and then uses Math.random() to create randomly generated sentences.

Goals:
 apply recursion to non-mathematical procedures
 understand basic ideas of context-free grammars
 apply data-structures